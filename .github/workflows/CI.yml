# CI disabilitato - Vercel gestisce build e deploy
# Riattivare se servono test automatici su PR

name: CI

on:
  pull_request:
    branches: [main]

jobs:
  build:
    if: false  # Disabilitato
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI disabled"
