/**
 * PricingInformation - Non utilizzato in Tradelia
 * Mantenuto per compatibilità con il boilerplate
 */

import React from 'react';

export const PricingInformation = (_props: {
  buttonList: Record<string, React.ReactNode>;
}) => {
  // Non utilizzato - Tradelia è gratuita
  return null;
};
